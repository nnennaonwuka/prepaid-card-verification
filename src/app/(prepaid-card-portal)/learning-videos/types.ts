export type LearningVideosData = {
  id: string;
  title: string;
}

export type SingleLearningVideoData = {
  title: string;
  url: string;
}

export type LearningVideosProps = {
  videoId: string;
  videoTitle: string;
}
